# 本地声音克隆（0.6B / 1.7B）

## 边生成边试听（2026-09-19）

- 点击“生成声音”后，首段音频到达便自动播放，后续片段连续接上；首次加载模型和编码预热仍需等待。开始播放或缓冲耗尽后预留约 0.25 秒缓冲，生成慢于播放时会显示“缓冲中”。
- 作品区显示已接收音频和缓冲时长；“暂停试听 / 继续试听”只控制播放，生成继续进行。点击历史作品、播放其他音频或开始新任务会停止此前的实时试听。
- 生成完成后继续播完已缓冲的内容，同时保存完整 WAV 和参数到作品库，可重播、下载。刷新或关闭网页会中断当前试听，但服务端继续生成并保存，之后可从作品库播放。
- 浏览器需支持 Web Audio 和流式 fetch，推荐使用新版 Chrome / Edge。实时试听由点击生成触发；若浏览器暂停了音频，可点“继续试听”。
- 后端新增 `/generate/stream`，逐行发送音频片段、完成结果或错误；原 `/generate` 接口保持兼容。前端播放逻辑位于 `studio-stream.js`，不改变模型参数及音频分块方式。

回归检查：`.\.venv\Scripts\python.exe -m unittest test_studio test_stream -v`、`node --test test_stream_player.js`、`node --check studio.js`、`node --check studio-stream.js`。

本次验证：12 项 Python 测试和 4 项前端测试通过，覆盖首段提前到达、错误/断线、暂停续播调度和缓冲耗尽。浏览器实测 0.6B 预热后约 0.93 秒收到首段，10.44 秒生成 23.2 秒音频；1.7B 切换加载后 6.52 秒收到首段、12.04 秒请求完成，推理 7.10 秒生成 11.12 秒音频，流式片段与保存 WAV 在 PCM16 量化精度内一致。1366×768 无整页溢出，390px 无横向溢出。这些是本次测量，首次加载/预热可能更慢，扬声器实际听感需人工确认。

## 声场工作台（2026-09-18）

启动方式不变，打开 http://127.0.0.1:7860 即可使用新的浅色工作台。

### 浅色监控版更新

- 顶栏缩为单行，底部扩大为折线图与任务进度两块。CPU 使用率、GPU 使用率、温度、功率展示最近 60 秒真实采样；GPU 使用率标题可切换为显存曲线。悬停曲线可看时间和数值，刷新页面仍可获取服务端保留的历史。无数据时留空，不填造数据。
- CPU 使用率由现有环境中的 `psutil` 采集；GPU 数据来自 `nvidia-smi`。本机当前接口未提供 CPU 温度、CPU 功率和 GPU 风扇转速，页面明确显示未提供。
- 任务区用带名称的步骤、当前高亮和完成勾选展示进度，同时显示当前阶段耗时、任务总耗时、已生成音频时长。真实模型加载耗时独立保留；复用模型不再把它覆盖成 0.00 秒，页面注明“复用模型”。最近加载记录保留到服务退出。
- 已加载所选模型时，加载按钮变灰；选择其他模型可“切换并加载”。没有加载任何模型时卸载按钮变灰，编辑文字不会重新启用它。
- 删除弹窗可选“移入回收站”或“永久删除”。前者保留在 `generated/.trash`，后者直接删除所选 WAV 与对应 JSON，无法从该回收站恢复。
- **500 字来源**：这是早期本地入口 `local_app.py` 的单次输入限制，在 `backups/before-1.7B-20260918-174049/local_app.py` 第 23–24 行已有，并非本轮新增。没有找到上游强制 500 字的依据。上游 `generate_voice_clone` / streaming 的 `max_new_tokens=2048` 是输出 token 上限，不是字符数；本地入口另设 1200 输出 token 上限。当前保留 500 字行为，界面明确标注“本地单次限制”。扩大输入长度需要另行考虑分段、上下文和输出截断。

本轮验证：8 项自动化测试通过；实际验证两种删除方式（只操作专门创建的测试文件）、CPU/GPU 采样、模型加载/复用/卸载按钮、切换选择后即时更新、生成阶段高亮。加载 0.6B 用时 5.32 秒，后续复用生成后仍显示 5.32 秒；1366×768 下整页与参数区无溢出，390px 小屏无横向溢出。改动前文件备份在 `backups/before-light-studio-*`。折线图及任务步骤的前端逻辑位于新增的 `studio-monitor.js`。

- **声音素材**：左侧选择、试听素材；点击“添加”上传 WAV、MP3、FLAC、OGG。每条素材分别保存人物名称和准确录音原文，编辑后点“保存素材信息”。支持同一人物的多条素材。新上传限制 30 MB / 180 秒，推荐 5–30 秒清晰单人录音。原有文件不会改名或覆盖。
- **生成作品**：右侧列出全部 WAV，包括旧版本生成的作品。选择后播放、下载，或复用该作品的文本、参考原文和参数。选择“移入回收站”后，恢复时将 `generated/.trash/<独立目录>/` 内的两个文件移回 `generated`，然后刷新作品列表；选择“永久删除”则不保留恢复副本。
- **参数**：中间“生成参数”标签支持语言、温度、Top-K、Top-P、重复惩罚、生成上限、种子、主生成随机采样、仅参考音色，以及实验性风格指令。种子 `-1` 每次随机，实际种子随作品保存。原文留空时自动仅参考音色；填写时应与音频准确对应。参数悬停可查看说明。
- **参数边界**：温度 0.1–2、Top-K 0–200、Top-P 0.01–1、重复惩罚 1–2、生成上限 16–1200。上限过小可能截断结尾，达到上限会提示。固定种子有助于对比，不保证跨模型、驱动版本结果完全一致。当前上游 CUDA 图的子预测器仍使用其固定采样配置；页面采样设置作用于主生成。风格指令是上游实验能力，效果需试听确认。
- **监控**：每秒读取显卡利用率、整卡显存、温度、功率和可用的风扇信息。设备不支持的数值显示 `—`。展示真实加载、参考处理、编码/预热、生成、保存阶段，以及已产出音频秒数、tokens、加载与总耗时，不提供虚构完成百分比。首轮生成时间包含 CUDA 图预热。
- **布局**：1440×900 与 1366×768 已检查，桌面整页不滚动，素材和作品列表内部滚动。生成参数通过标签切换；手机采用纵向布局。

新页面为 `studio.html`、`studio.css`、`studio.js`，后端为 `local_app.py` 和 `studio.py`。旧 `local-ui.html` 保留，主页已使用新版。素材元数据保存在 `test-voice/library.json`，输入草稿保存在当前浏览器。改造前入口和说明已备份到 `backups/before-studio-*`。

推理调用上游 `generate_voice_clone_streaming`，每个实际音频片段立即发送到网页试听，同时收集形成完整 WAV 并显示实时进度；相同参数与旧版一次性解码的音频不保证逐样本一致。仍只监听本机 127.0.0.1，不需要远程服务。

验收：5 项库与校验测试通过；实际浏览器上传、编辑、刷新恢复、选择播放、参数设置、可恢复删除通过。0.6B 与 1.7B 均完成真实生成；1.7B 完整参考测试生成 20.24 秒音频，推理 12.00 秒。连续监控测试生成 13.12 秒音频，推理 7.36 秒，观察到 loading / encoding / generating 阶段，状态请求最大约 0.187 秒。这些是当次实测，不是性能保证。浏览器播放无解码错误；音色自然度与相似度尚需人工试听。

本地回归检查：

```powershell
.\.venv\Scripts\python.exe -m unittest test_studio test_stream -v
.\.venv\Scripts\python.exe -m py_compile local_app.py studio.py
node --test test_stream_player.js
node --check studio.js
node --check studio-stream.js
```

安装位置：D:\Projects\fast-qwen3-tts

## 使用

1. 双击 start.cmd 或 start.bat，保持窗口打开。
2. 浏览器访问 http://127.0.0.1:7860 。
3. 选择 0.6B 或 1.7B 模型。可以点“加载所选模型”预先放进显存，或直接点“生成声音”（会自动加载）。
4. 暂时不用时，在网页点“卸载模型”，服务仍在，显存会释放。彻底退出请双击 stop.cmd 或 stop.bat。
5. 生成后可直接试听、下载；文件也保存在 generated 目录。

只关闭 start 窗口不够：实际占用显存的是后面的 Python 进程。请用 stop.cmd / stop.bat，它会先让服务自行卸载模型，再结束整个进程树。服务仅监听本机，不开放到局域网。

“模板录音的准确文字”可以留空，使用音色参考模式。如果填写，必须与所选录音内容准确对应，使用完整参考模式。朗读文字与模板原文是两个不同的输入。生成质量、口音和相似程度需要实际试听确认。

## 隔离方式

runtime：本软件独立 Python；.venv：独立依赖；models：模型；cache：下载/计算缓存；tmp：临时文件；tools：本地包管理工具；app：原项目源代码；test-voice：你的录音；generated：生成的音频。

未修改系统 PATH，未安装全局 Python 软件包，未设置开机自启或系统服务。environment.ps1 中的环境变量只作用于当前启动进程及其子进程。

## 卸载

先关闭服务窗口，再备份你要保留的 test-voice 和 generated。然后删除 D:\Projects\fast-qwen3-tts 整个文件夹即可卸载本次安装的程序、模型和依赖。

## 来源

https://github.com/andimarafioti/faster-qwen3-tts
源代码提交：e2a215f61984c0e72a242f8dd72333338e7672f4
模型：Qwen/Qwen3-TTS-12Hz-0.6B-Base

local_app.py 与 local-ui.html 是此次配置的本机中文操作入口，推理使用原项目 FasterQwen3TTS。

## 本次安装说明

- Transformers 固定为 5.15.1：自动解析到的 5.17.0 在加载 Mimi 音频组件时出现 rope_theta 错误。
- 运行时强制离线加载已经下载的模型。下载脚本只用于安装或修复下载。
- 停止服务请双击 stop.cmd 或 stop.bat，不要只关窗口。网页里也可以卸载/加载模型。
- 参考 MP3 会在 cache/references 内生成单声道 WAV 副本，原文件不改动。
- 不需要 Flash Attention。SoX 缺失提示来自上游导入，本次音频读取使用 soundfile。

## 实测结果（2026-09-18）

RTX 4060 Laptop 8GB，PyTorch 2.11.0+cu128，Transformers 5.15.1。
模型文件 SHA-256 与 Hugging Face 缓存标识一致，依赖检查通过。
使用用户提供的 30 秒 MP3，音色参考模式成功生成 24kHz 单声道 WAV。
首次含预热推理 18.23 秒；重启并复用编译缓存后 7.84 秒；同进程再次推理 4.50 秒，生成 10.88 秒音频。
显存占用约 3029 MiB（当次整张显卡读数）。
浏览器音频 readyState=4，无解码错误；页面生成按钮与下载链接正常。未由人工评价音色相似度。
最新试听：generated\20260918-163730-487900.wav。
服务只监听 127.0.0.1:7860；本次保留后台服务供直接试听，使用 stop.cmd 可停止。

## 0.6B / 1.7B 切换（2026-09-18 更新）

网页支持手动选择 0.6B Base 或 1.7B Base。选好模型后可以“加载所选模型”，或直接“生成声音”。

- 8GB 显卡每次只保留一个模型；加载另一个或点“卸载模型”时会先释放旧模型。切换后的第一次生成包含预热，时间不能直接作为稳定推理速度比较。
- “卸载模型”只释放显存，服务仍可在浏览器里继续用；彻底关闭请用 stop.cmd / stop.bat。
- 新结果文件名标记 0.6B 或 1.7B；对应 JSON 保存模型、输入、参考原文、用时和随机种子。旧结果都保留。
- 页面会在当前浏览器本地保存输入草稿。

实测（同一段默认中文、同一模板、音色参考模式）：1.7B 首次含加载预热 14.12 秒，推理 7.54 秒，生成 10.56 秒音频，显存约 4943 MiB；随后切回 0.6B 含加载 6.89 秒，推理 5.18 秒，生成 11.2 秒音频，显存回到约 3053 MiB，未出现两个模型叠加占用。试听文件：generated\20260918-180512-1.7B-239800.wav 与 generated\20260918-180544-0.6B-906600.wav。

1.7B 模型：Qwen/Qwen3-TTS-12Hz-1.7B-Base
固定版本：fd4b254389122332181a7c3db7f27e918eec64e3
下载目录：models\huggingface，与原模型共用隔离环境。
重新下载/修复时，在本目录 PowerShell 中运行：
. .\environment.ps1
.\.venv\Scripts\python.exe .\download_model.py --model 1.7B

运行和停止仍使用 start.cmd / stop.cmd，卸载方式不变。

